安装：解压即可
目录
	1. Bin：启动，关闭和其他脚本。
	2. Conf：配置文件和相关的DTD
	3. Logs：日志文件
	4. Webapps

Web.xml
元素
	1. welcome-file-list
	2. <filter>：过滤器
	3. <lister>：监听器，监听对象变化
	4. servlet：web应用类、servlet-name、servlet-class
	5. servlet-mapping：servlet-name、url-pattern

*配置文件中的所有信息在启动时才被读取，所以改动文件后，必须重启容器才能使之生效。

版本
1.当前版本8
